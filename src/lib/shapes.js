const randomBetween = (min, max) => min + Math.random() * (max - min);
function star(count) { return Array.from({ length: count }, () => { const angle = randomBetween(0, Math.PI * 2), segment = angle / (Math.PI * 2) * 10, fraction = segment - Math.floor(segment), a = Math.floor(segment) % 2 === 0 ? 42 : 19, b = Math.floor(segment) % 2 === 0 ? 19 : 42, radius = (a + (b - a) * fraction) * randomBetween(0.9, 1.05); return [50 + radius * Math.cos(angle), 50 + radius * Math.sin(angle)]; }); }
function crescent(count) { const points = []; while (points.length < count) { const angle = randomBetween(0, Math.PI * 2), radius = randomBetween(0, 40), point = [50 + radius * Math.cos(angle), 50 + radius * Math.sin(angle)]; if (Math.hypot(point[0] - 66, point[1] - 50) >= 24) points.push(point); } return points; }
function blobs(count) { return Array.from({ length: count }, (_, index) => { const [cx, cy] = index % 2 ? [74, 62] : [28, 40], angle = randomBetween(0, Math.PI * 2), radius = Math.abs(randomBetween(-1, 1)) * 16; return [cx + radius * Math.cos(angle), cy + radius * Math.sin(angle)]; }); }
function scatter(count) { return Array.from({ length: count }, () => { const angle = randomBetween(0, Math.PI * 2), radius = Math.sqrt(Math.random()) * 40; return [50 + radius * Math.cos(angle), 50 + radius * Math.sin(angle)]; }); }
export const SHAPES = { star, crescent, blobs, scatter };
